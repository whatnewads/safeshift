# SafeShift EHR - Frontend Environment Variables

# API Base URL
# Development: Leave empty (uses Vite proxy)
# Production: Set to your production API URL
VITE_API_URL=

# API Host for Vite Proxy (Development only)
# Default: http://localhost:8000 (works when both PHP and npm run in Windows)
# WSL: Set to Windows host IP when running npm in WSL with PHP in Windows
#      To find Windows host IP from WSL: cat /etc/resolv.conf | grep nameserver | awk '{print $2}'
#      Example: http://172.22.192.1:8000
VITE_API_HOST=

# Application name
VITE_APP_NAME=SafeShift EHR

# Session timeout in seconds (for session warning)
VITE_SESSION_TIMEOUT=3600

# Session warning threshold in seconds (show warning before timeout)
VITE_SESSION_WARNING=300

# Environment mode
VITE_ENV=development
